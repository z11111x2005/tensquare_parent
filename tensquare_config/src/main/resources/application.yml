server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/liuympoj/tensquare_dev.git
  rabbitmq:
    host: 129.204.200.206
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh